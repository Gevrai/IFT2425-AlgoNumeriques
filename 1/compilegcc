/*echo */
/*echo compilation du programme ${a=${1}.c}*/

/*g++ -o $1 $a -lm -lX11  */

echo 
echo compilation du programme ${a=${1}.c}

g++ -o exec/$1 $a -lm -lX11  

echo
echo execution
echo
exec exec/$1
